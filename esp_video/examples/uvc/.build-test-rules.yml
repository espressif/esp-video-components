esp_video/examples/uvc:
  enable:
    - if: IDF_TARGET in ["esp32p4", "esp32s3"]
      reason: only support on esp32p4 and esp32s3
  depends_components:
    - esp_video
    - esp_cam_sensor
    - esp_sccb_intf